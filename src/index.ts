

/**
 *  获取一个数量
 * @param list
 * @example ```ts```
 * 
 * ```
 *  let a = '123'
 *   function getList(list: string) {
 *      return list;
 *   }
 *   getList(['1',2, 3]) => [1]
 * ```
 * @returns 返回依赖
 */
function getList(list: string) {
  return list;
}

export default {
  getList,
};
